# マジョリンスイーツ サイト修正依頼書 v4.1

**プロジェクト名**: マジョリンスイーツ(MAJORIN SWEETS / MAJORINS)
**発行日**: 2026-05-20
**最終更新**: 2026-05-22(v4.1でオープニング動画ファイル確定 / 5.6秒 / opening.mp4)
**依頼元**: 平石さん(クライアント)/ Kei
**実行**: Cursor AI(一括作業)
**ステータス**: 実装着手可

---

## 📋 0. 概要

マジョリンスイーツのブランドサイトに対し、**春シーズン向けアップデート + 体験演出強化** を一括実施する。
主な変更は以下の7カテゴリ:

1. **オープニング演出(新規)**: ページ読み込み時に動画再生 → ホワイトアウト → トップページがフェードイン
2. **トップヒーロー**: 画像差し替え(2枚スライダー化)+ キャプション変更
3. **ようこそ画面(執事セレクト)**: 文言変更 + カラー変更(黄→黒系)
4. **パウンドケーキ販売箇所**: 暫定イメージ画像に差し替え
5. **占い機能**: 既存「バブル→写真消失」を「バブル→手紙ポップアップでマジョリンのメッセージ表示」に変更 + メッセージ **全50種** を実装
6. **STORYセクション**: 見出し・本文の文言刷新
7. **「魔法で作ったもの〜」セクション**: 画像を春用に差し替え
8. **サイト全体カラートーン**: 黄色排除、ピンク+モノトーンで統一

---

## 🎨 1. 全体カラートーン方針

### 削除する色
- **黄色系全般**(現行アクセントカラー)

### 採用する基調色

| 用途 | 色 | コード(目安) |
|---|---|---|
| メインテキスト | 黒 | `#1A1A1A` |
| サブテキスト・枠線 | 濃いグレー | `#333333` 〜 `#4A4A4A` |
| アクセント | ロゴカラーのピンク | **`{{PINK_LOGO_COLOR}}` ← 確定値を後日反映**(暫定 `#C04060`) |
| 背景 | 現行を踏襲 | - |

> ピンクの確定カラーコードは平石さんから別途共有予定。確定後に `{{PINK_LOGO_COLOR}}` を全置換する。

---

## 📝 2. タスク一覧

| # | ID | 対象 | 変更内容 | 種別 | 優先度 |
|---|---|---|---|---|---|
| 1 | **H-1** | **オープニング演出(新規)** | **ページ読み込み時の動画再生 → ホワイトアウト → フェードイン** | **機能新規実装** | **高** |
| 2 | A-1 | トップヒーロー(画像) | 静止画 → 春用画像2枚のクロスフェードスライダー | 画像差替 + 機能追加 | 高 |
| 3 | A-2 | トップヒーロー(文言) | キャプション本文を新コピーに差し替え | 文言変更 | 高 |
| 4 | A-3 | トップヒーロー(スタイル) | キャプション本文を白文字+黒フチに | スタイル変更 | 高 |
| 5 | B-1 | ようこそ画面(文言) | 見出し・サブテキストを新コピーに | 文言変更 | 高 |
| 6 | B-2 | ようこそ画面(色) | 黄色 → 黒/濃グレーに置換 | スタイル変更 | 高 |
| 7 | B-3 | ようこそ画面(ピンク) | サブテキストにロゴ基準ピンク適用 | スタイル変更 | 高 |
| 8 | C-1 | パウンドケーキ販売箇所 | 暫定イメージ画像に差し替え | 画像差替 | 中 |
| 9 | D-1 | 占い機能(挙動) | バブル弾く → 手紙ポップアップ表示に変更 | 機能変更 | 高 |
| 10 | D-2 | 占い機能(メッセージ) | メッセージ全50種を実装(配列データ) | データ実装 | 高 |
| 11 | D-3 | 占い表示カード(キャプション) | 3枚カードのタイトル・説明文を変更 | 文言変更 | 高 |
| 12 | D-4 | 占いセクション(リード文) | 画像下に新リード文を追加 | 文言変更 | 高 |
| 13 | E-1 | STORYセクション(文言) | 見出し・章構成・本文を刷新 | 文言変更 | 高 |
| 14 | F-1 | 「魔法で作ったもの〜」 | 画像を春用に差し替え | 画像差替 | 中 |
| 15 | G-1 | サイト全体カラートーン | 黄色排除、ピンク+モノトーン統一(横断) | スタイル変更 | 中 |

---

## 📂 3. 画像・動画アセット一覧

すべて `public/assets/spring2026/` 配下に配置(Next.jsの `<Image>` `<video>` で利用)。

| ファイル名 | 用途 | 内容 | 提供状況 |
|---|---|---|---|
| `opening.mp4` | オープニング演出 | マジョリンスイーツの店に来店するイメージ動画(無音、**5.6秒**、H.264 MP4、1440×1080、1.63MB) | ✅ 提供済み(変換済) |
| `hero_spring_01.png` | トップヒーロー スライド1 | 薔薇の咲くコテージ前 / ピンクカーディガン姿の女性 | ✅ 提供済み |
| `hero_spring_02.png` | トップヒーロー スライド2 + 「魔法で作ったもの」流用 | キッチンで2段ケーキをデコレーション、白フードの女性 | ✅ 提供済み |
| `pound_cake_showcase_temp.png` | パウンドケーキ販売箇所(暫定) | ショーケース風ディスプレイ(Berry / Chocolate Citrus / Matcha) | ✅ 提供済み |

---

## 🛠 4. 修正タスク詳細

---

### H. オープニング演出(新規追加 ★最重要★)

#### H-1. ページ読み込み時の演出シーケンス

**目的**: マジョリンスイーツのブランド世界観への没入感を提供。「店に来店する体験」をデジタル上で再現する。

**演出フロー**:

```
[ページ読み込み開始]
        ↓
[STAGE 1] 動画フル画面再生(5.6秒、無音、自動再生、スキップ不可)
        ↓
[STAGE 2] 動画終了 → ホワイトアウト(0.8秒で白フェードイン)
        ↓
[STAGE 3] ホワイトアウトが消える(1秒で白フェードアウト)
        同時に、トップページ全体がフェードイン(opacity: 0 → 1)
        ↓
[完了] 通常のトップページが表示される
```

#### H-2. 仕様詳細

```yaml
動画:
  ファイル: public/assets/spring2026/opening.mp4
  フォーマット: MP4 (H.264 / High Profile / Level 4.0)
  解像度: 1440×1080
  長さ: 5.6秒
  音声: なし(変換時に削除済み、HTML側でも muted 属性を付与)
  ファイルサイズ: 1.63 MB
  表示頻度: 毎回(ページリロード毎に再生)
  スキップボタン: なし(強制視聴)
  内容: マジョリンスイーツの店に来店するイメージ
  
表示方法:
  全画面オーバーレイ(position: fixed, top:0, left:0, width:100vw, height:100vh)
  z-index: 9999(全要素の上)
  object-fit: cover(画面サイズに合わせてトリミング)
  
モバイル対応:
  playsinline属性必須(iOS Safari全画面化防止)
  autoplay属性
  muted属性
  preload="auto"(動画ロード遅延を防ぐ)
  
ホワイトアウト演出:
  色: #FFFFFF
  Stage 2(動画→ホワイト): opacity 0 → 1 を 0.8秒で
  Stage 3(ホワイト→ページ): opacity 1 → 0 を 1秒で
  
トップページのフェードイン:
  Stage 3 と同時進行
  body または main wrapper の opacity: 0 → 1 を 1秒で
  初期状態は完全に見えない → 徐々にうっすら見えてくる → 完全表示
```

#### H-3. 実装ヒント(Next.js 14 / TypeScript)

```tsx
// components/OpeningAnimation.tsx
'use client';

import { useEffect, useRef, useState } from 'react';

type Stage = 'video' | 'whiteout' | 'fadein' | 'done';

export const OpeningAnimation = ({ onComplete }: { onComplete: () => void }) => {
  const [stage, setStage] = useState<Stage>('video');
  const videoRef = useRef<HTMLVideoElement>(null);

  // 動画終了 → ホワイトアウトへ
  const handleVideoEnded = () => setStage('whiteout');

  // ホワイトアウト完了 → フェードインへ
  useEffect(() => {
    if (stage === 'whiteout') {
      const t = setTimeout(() => setStage('fadein'), 800);
      return () => clearTimeout(t);
    }
    if (stage === 'fadein') {
      const t = setTimeout(() => {
        setStage('done');
        onComplete();
      }, 1000);
      return () => clearTimeout(t);
    }
  }, [stage, onComplete]);

  if (stage === 'done') return null;

  return (
    <div className="fixed inset-0 z-[9999] pointer-events-none">
      {/* Stage 1: Video */}
      {stage === 'video' && (
        <video
          ref={videoRef}
          src="/assets/spring2026/opening.mp4"
          autoPlay
          muted
          playsInline
          preload="auto"
          onEnded={handleVideoEnded}
          className="w-full h-full object-cover"
        />
      )}
      {/* Stage 2-3: Whiteout overlay */}
      {(stage === 'whiteout' || stage === 'fadein') && (
        <div
          className={`absolute inset-0 bg-white transition-opacity duration-700 ${
            stage === 'fadein' ? 'opacity-0' : 'opacity-100'
          }`}
        />
      )}
    </div>
  );
};
```

```tsx
// app/page.tsx(またはレイアウト)
'use client';
import { useState } from 'react';
import { OpeningAnimation } from '@/components/OpeningAnimation';

export default function HomePage() {
  const [openingDone, setOpeningDone] = useState(false);

  return (
    <>
      <OpeningAnimation onComplete={() => setOpeningDone(true)} />
      <main
        className={`transition-opacity duration-1000 ${
          openingDone ? 'opacity-100' : 'opacity-0'
        }`}
      >
        {/* ← トップページの中身 */}
      </main>
    </>
  );
}
```

#### H-4. 注意点

- **必ず `muted` + `playsInline` + `autoPlay`** をセットで使用(iOS/Android対応)
- 動画ロードが遅い場合、初期表示が黒画面になる可能性 → `preload="auto"` で先読み
- Stage 3 でトップページがフェードインする際、**`pointer-events: none`** を解除するタイミングに注意(ボタン押下できなくなる事故を防ぐ)
- SSR配慮: `'use client'` 必須
- **動画ファイルは既にWeb最適化済み**(MP4/H.264/faststart/無音/1.63MB)、追加変換不要

---

### A. トップヒーローセクション

#### A-1. 画像のスライダー化

**現状**: 静止画1枚(コテージ前の女性、ロングコート姿)
**変更後**: 春用画像2枚をクロスフェードで切り替え

**スライダー仕様**:
```yaml
画像:
  - public/assets/spring2026/hero_spring_01.png
  - public/assets/spring2026/hero_spring_02.png
切り替え方式: クロスフェード(opacity transition)
切り替え間隔: 5秒
自動再生: ON
ループ: ON
インジケーター: 下部に小さくドット表示(任意)
モバイル: スワイプ操作対応
```

#### A-2. キャプション文言変更

**配置・フォント・レイアウト**: 現行と同一を維持(変更はテキスト内容のみ)

**新コピー**:
```
完璧な人生などない、
でもその一瞬を輝かせるマジョリンスイーツ

魔法の扉を開けて、MAJORINS の世界へ
```

#### A-3. キャプション色仕様

- **「MAJORIN SWEETS」ロゴ部分**: 現行色のまま据え置き
- **キャプション本文**: 白文字 + 黒フチ(text-stroke)

```css
.hero-caption-body {
  color: #FFFFFF;
  -webkit-text-stroke: 1px #000000;
  text-stroke: 1px #000000;
  text-shadow: 0 0 2px rgba(0, 0, 0, 0.6);
}
```

---

### B. ようこそ画面(執事セレクト)

#### B-1. 文言変更

**現行**:
```
ようこそ、マジョリンの世界へ
執事がお迎えいたします。
どちらをお選びになりますか?
```

**変更後**:
```
ようこそ MAJORINS の世界へ
私がご案内いたします
どちらをお選びになりますか?
```

#### B-2. カラー変更

| 要素 | 現行 | 変更後 |
|---|---|---|
| 見出し「ようこそ MAJORINS の世界へ」 | 黄色 | **黒 `#1A1A1A`** または **濃グレー `#333333`** |
| 選択ボタン(4ボタン)の囲み枠 | 黄色 | **黒 `#1A1A1A`** または **濃グレー `#333333`** |
| ボタン内テキスト | 黄色 | **黒 `#1A1A1A`** または **濃グレー `#333333`** |
| サブテキスト「私がご案内いたします / どちらをお選びになりますか?」 | (新規) | **ピンク `{{PINK_LOGO_COLOR}}`** |

---

### C. パウンドケーキ販売箇所

#### C-1. 画像差し替え(暫定)

```jsx
{/* 暫定画像。本物商品画像と後日差し替え予定(別チケット化) */}
<Image
  src="/assets/spring2026/pound_cake_showcase_temp.png"
  alt="マジョリンパウンドケーキ(イメージ)"
/>
```

---

### D. 占い機能 ★重要変更★

#### D-1. インタラクション挙動の変更

**現状**: 画面にマジョリンの写真がバブル(球体)として浮かぶ → 弾くと写真が消える
**変更後**: バブルを弾く → **手紙のポップアップが浮き上がる** → マジョリンからのメッセージが表示

```yaml
ポップアップ仕様:
  形式: 中央付近に浮き上がる手紙風カード
  アニメーション:
    出現: フェードイン + スケール(0.8 → 1.0) + 上方向に浮上(translateY: 20px → 0)
    消滅: フェードアウト + 上方向フェードオフ
  デザイン:
    手紙風レイアウト(古紙テクスチャ背景、罫線、手書き風フォント推奨)
    深いピンク・モノトーン基調
  閉じる動作: 外側クリック / ✕ボタン / ESCキー / 数秒で自動消滅(任意)
  メッセージ選定: 50件の配列からランダム抽選
```

#### D-2. メッセージデータ(全50件)

メッセージプールは **全50件** を実装する。下記をデータファイル `data/marjorinMessages.ts` として実装。

```typescript
// data/marjorinMessages.ts
export type MarjorinMessage = {
  id: number;
  body: string;
  tone?: 'devil' | 'philosopher' | 'human' | 'mixed';
};

export const MARJORIN_MESSAGES: MarjorinMessage[] = [
  // --- 既存10件 ---
  { id: 1, body: "完璧な人生などない。でも、その一瞬を輝かせることは、あなたにもできる。", tone: "mixed" },
  { id: 2, body: "痛みを消すのではなく、痛みと共に歩む力を、そっと渡したい。", tone: "philosopher" },
  { id: 3, body: "夢は遠くにあるのではない。今日の一口の甘さの中にある。", tone: "mixed" },
  { id: 4, body: "勝者でなくていい。心があるものの方が、ずっと強い。", tone: "philosopher" },
  { id: 5, body: "手で作ったものだけが、心に残る。魔法ではなく、あなたの時間を。", tone: "philosopher" },
  { id: 6, body: "不器用な形のケーキほど、温かいことがある。", tone: "human" },
  { id: 7, body: "真実の一言は、50枚のカードの中に、一枚だけある。", tone: "mixed" },
  { id: 8, body: "お菓子は、あなたを癒す魔法。杖は、もう置いた。", tone: "philosopher" },
  { id: 9, body: "MAJORINS の扉を開けたあなたへ——ようこそ、甘さの世界へ。", tone: "mixed" },
  { id: 10, body: "輝くパッションを、心に響くスイーツで届けたい。", tone: "mixed" },

  // --- 追加40件:Devil(鋭く核心を突く) ---
  { id: 11, body: "言い訳の数だけ、あなたから自由が遠ざかる。", tone: "devil" },
  { id: 12, body: "迷っている時点で、答えはもう出ている。それを認める勇気だけが、足りないの。", tone: "devil" },
  { id: 13, body: "「できない」は、たいてい「やりたくない」の上品な言い換え。", tone: "devil" },
  { id: 14, body: "完璧を装うのは、もう、おやめなさい。", tone: "devil" },
  { id: 15, body: "期待された通りに生きて、それで満足? ——本当に?", tone: "devil" },
  { id: 16, body: "「忙しい」という言葉に、私はもう騙されない。あなたも、騙されないで。", tone: "devil" },
  { id: 17, body: "強がりは美しくない。素直さの方が、ずっと品がある。", tone: "devil" },
  { id: 18, body: "諦める理由を、他人に求めないで。", tone: "devil" },
  { id: 19, body: "自分に嘘をついた数だけ、夜が長くなる。", tone: "devil" },
  { id: 20, body: "あなたのベストは、本当にそれ?", tone: "devil" },

  // --- 追加40件:Philosopher(人生哲学) ---
  { id: 21, body: "痛みを抱えたまま、歩いていい。消さなくていい。", tone: "philosopher" },
  { id: 22, body: "完璧な選択など、どこにもない。選んだ道を、完璧にしていくだけ。", tone: "philosopher" },
  { id: 23, body: "強さとは、弱さを許すこと。50年生きて、やっとわかった。", tone: "philosopher" },
  { id: 24, body: "沈黙は、時に最も雄弁な答えになる。", tone: "philosopher" },
  { id: 25, body: "失敗を恥じる時間で、もう一度、やり直せる。", tone: "philosopher" },
  { id: 26, body: "過去を消したくなる夜こそ、未来を作る材料になる。", tone: "philosopher" },
  { id: 27, body: "自分を救えるのは、結局、自分の手だけ。誰の魔法でもない。", tone: "philosopher" },
  { id: 28, body: "完璧な誰かより、不器用な本物のあなたの方が、ずっと尊い。", tone: "philosopher" },
  { id: 29, body: "「今のままじゃダメ」と思ったその瞬間に、もう、変わり始めている。", tone: "philosopher" },
  { id: 30, body: "真実は、優しくない。でも、嘘よりずっと、あなたを生かす。", tone: "philosopher" },

  // --- 追加40件:Human(温かく弱さを見せる) ---
  { id: 31, body: "私だって、生地を何度もダメにする。それでも、もう一度、混ぜ始めるの。", tone: "human" },
  { id: 32, body: "顔にチョコをつけたまま、笑える日が、いちばん尊い。", tone: "human" },
  { id: 33, body: "「まぁ、いっか」と呟ける夜が、本当の強さの始まり。", tone: "human" },
  { id: 34, body: "不器用な指でも、誰かの心に、温かさは届く。", tone: "human" },
  { id: 35, body: "一人の夜のお茶は、誰かを思う時間でもある。", tone: "human" },
  { id: 36, body: "弱さを見せていい場所が、人生に一つあれば、それで十分。", tone: "human" },
  { id: 37, body: "泣いた跡のあるあなたの顔を、私は美しいと思う。", tone: "human" },
  { id: 38, body: "立ち止まっていい。立ち止まった人にしか、見えない景色がある。", tone: "human" },
  { id: 39, body: "急がなくていい。お菓子も、人生も、待つ時間が美味しさを作る。", tone: "human" },
  { id: 40, body: "「もう、無理」と言える勇気の方が、頑張る勇気より、ずっと難しい。", tone: "human" },

  // --- 追加40件:Mixed(お菓子・夢・選択) ---
  { id: 41, body: "一口のケーキが、明日を生きる理由になることがある。それで、十分。", tone: "mixed" },
  { id: 42, body: "「いつか」じゃない。今日、味わいなさい。", tone: "mixed" },
  { id: 43, body: "甘さは、逃げ場ではない。立ち直る前の、深呼吸。", tone: "mixed" },
  { id: 44, body: "手で混ぜる粉の重さの中に、魔法より確かなものがある。", tone: "mixed" },
  { id: 45, body: "ケーキの形が崩れても、込めた想いは崩れない。", tone: "mixed" },
  { id: 46, body: "願いを叶える魔法はもう、ない。でも、願いを抱き続ける力は、ここにある。", tone: "mixed" },
  { id: 47, body: "焦がした砂糖の苦さも、人生の調味料。捨てなくていい。", tone: "mixed" },
  { id: 48, body: "完璧なケーキを目指して、不完全に焼けたケーキを愛おしむ。それでいいのよ。", tone: "mixed" },
  { id: 49, body: "あなたの夢の重さは、あなただけが知っている。誰にも値踏みさせないで。", tone: "mixed" },
  { id: 50, body: "扉を開けたなら、もう、戻らなくていい。そのまま、進みなさい。", tone: "mixed" },
];

// ランダム抽選ユーティリティ
export const pickRandomMessage = (): MarjorinMessage => {
  const idx = Math.floor(Math.random() * MARJORIN_MESSAGES.length);
  return MARJORIN_MESSAGES[idx];
};
```

#### D-3. 占い表示カード(トップに見える3枚)のキャプション変更

| カード | 現行タイトル | 新タイトル | 新説明 |
|---|---|---|---|
| 1枚目 | 魔法は、置いた | **魔法は夢** | **貴方の夢はなんですか?** |
| 2枚目 | 不完全でいい | **勝者でなくてもいい** | **勝負ではなく 心があるものの方が強い** |
| 3枚目 | 真実の一言 | **(現状維持)** | **(現状維持)** |

#### D-4. 占いセクションのリード文

**現行(画像上、現状維持)**:
```
痛みを消さない。
痛みと生きる力をくれる。
```

**追加(画像下)**:
```
心に響くスイーツ。
あなたが輝くパッションを届けたい。
```

> ⚠️ TODO T3(平石さん確認中): 既存リード文「痛みを消さない〜」を残す解釈で実装。確認後に調整可能。

---

### E. STORYセクション

#### E-1. 見出し・章構成の刷新

```
STORY  ← 見出しは現行のまま

なぜマジョリンはお菓子を作ったのか

━━━━━━━━━━━━━━━━━━━━

【第1章】完璧だった過去
(本文は現行のままでOK ※平石さん「ここはOK」)

━━━━━━━━━━━━━━━━━━━━

【第2章】気づいた、真実
私の魔法の使い方は間違いだった
(追加本文は未確定。プレースホルダーで仮実装 ← TODO T4)

━━━━━━━━━━━━━━━━━━━━

【第3章】選んだ、道
だから、魔法の使い方を変えた
お菓子の魔法で
人の心を癒そうと。
```

---

### F. 「魔法で作ったもの〜」セクション

#### F-1. 画像差し替え

```jsx
<Image
  src="/assets/spring2026/hero_spring_02.png"
  alt="魔法で作ったものは、魔法で壊れる"
/>
```

> トップヒーロー スライド2と同一画像を流用。
> キャプション「魔法で作ったものは、魔法で壊れる。でも、手で作ったものは、心に残る」は現状維持。

---

### G. サイト全体のカラートーン統一

明示指示はようこそ画面のみだが、他セクションでも黄色アクセントがあれば洗い出して **ピンク + モノトーン** に統一。

実施手順:
1. プロジェクト全体で `yellow` `#FFFF` `#FFD` `gold` 系のクラス・カラーコードを grep
2. 文字色の黄色 → `#1A1A1A` または `{{PINK_LOGO_COLOR}}`
3. 枠線の黄色 → `#333333`
4. 横断置換後、視覚チェック

---

## ✅ 5. 実装手順(推奨順序)

1. **準備フェーズ**
   - [ ] 画像・動画アセットを `public/assets/spring2026/` に配置
   - [ ] 各セクションのコンポーネントファイルを特定

2. **H. オープニング演出** → タスク1
3. **A. トップヒーロー** → タスク2〜4
4. **B. ようこそ画面** → タスク5〜7
5. **D. 占い機能** → タスク9〜12
6. **E. STORYセクション** → タスク13
7. **F. 「魔法で作ったもの〜」** → タスク14
8. **C. パウンドケーキ** → タスク8
9. **G. 全体カラートーン統一** → タスク15(最後に横断チェック)

10. **検証フェーズ**
    - [ ] PC / モバイル両方で表示確認(iOS Safari、Android Chrome必須)
    - [ ] オープニング演出が毎回再生されることを確認
    - [ ] 動画ロード遅延がないか確認(初回キャッシュなし時)
    - [ ] スライダー動作確認
    - [ ] 占い機能の新挙動確認(バブル弾く → 手紙ポップアップ表示)
    - [ ] 占いメッセージが50件すべて表示可能か(ランダム抽選)
    - [ ] 黄色が残っていないか全体スキャン

11. **デプロイフェーズ**
    - [ ] ステージング環境にデプロイして平石さんに確認依頼
    - [ ] 修正指摘対応後、本番反映

---

## 🚧 6. 残TODO(平石さん確認待ち事項)

| ID | 項目 | ブロック度 | 状態 |
|---|---|---|---|
| **T1** | ロゴカラー基準のピンク(`{{PINK_LOGO_COLOR}}`)の確定値 | 中(暫定 `#C04060` で進行可) | Kei側で把握済み、後日反映 |
| **T2** | 占い機能の50件メッセージ | ✅ 解決済み(本書D-2に確定版を記載) | 完了 |
| **T3** | 占いセクションのリード文「痛みを消さない〜」を残すか/置き換えるか | 低(暫定「残す+追加」で実装) | 確認中 |
| **T4** | STORY第2章「気づいた、真実」の本文 | 中(プレースホルダーで仮実装可) | 平石さんから後日 |
| **T5** | 「魔法で作ったもの〜」セクションの画像はスライド2と同一でOKか | ✅ 解決済み(同一前提で実装) | 完了 |
| **T6** | パウンドケーキ販売箇所の本物画像差し戻しタイミング | 低(暫定画像で先行リリース可) | 別チケット化 |
| **T7** | スライダーの切替速度・黒フチの太さの最終承認 | 低(仮実装→デモで確認) | 仮実装後 |
| **T8** | オープニング動画ファイルの最終ファイル名・配置先 | ✅ 解決済み(`opening.mp4` で確定、配置先 `public/assets/spring2026/`) | 完了 |

---

## 📎 7. 参考資料

- LINE「平石さん」とのトーク(2026-05-20)
- スクリーンショット9枚(IMG_1300 〜 IMG_1308)
- マジョリンスイーツ_完全版ブランドコンセプト.md
- ブランド名表記: **MAJORIN SWEETS** / **MAJORINS** / **マジョリン** / **マジョリンスイーツ**(文脈で使い分け)

---

## 💬 8. Cursor向け実行プロンプト(コピー&ペースト用)

```
このマジョリンスイーツの修正依頼書(majorin_update_spec_v4.md)に従って、以下を順番に実装してください:

【前提】
- Next.js 14 / TypeScript プロジェクト
- public/assets/spring2026/ 配下にアセット配置済み
- ピンクは {{PINK_LOGO_COLOR}} 変数で表現、暫定値 #C04060
- TODO項目は // TODO: T1 〜 T8 コメントでコード内に残す

【実装順】
1. H. オープニング演出:
   - OpeningAnimation.tsx を新規作成
   - 動画フル画面再生(opening.mp4、無音、5.6秒、毎回再生、スキップなし)
   - 動画終了 → ホワイトアウト(0.8秒) → ホワイト消失と同時にトップページがフェードイン(1秒)
   - playsInline / muted / autoPlay / preload="auto" 必須

2. A. トップヒーロー:
   - 春用画像2枚のクロスフェードスライダー(5秒間隔、ループ、モバイルスワイプ対応)
   - キャプション本文を新コピーに差し替え + 白文字+黒フチ(text-stroke 1px)

3. B. ようこそ画面:
   - 文言を新コピーに差し替え
   - 黄色 → #1A1A1A または #333333
   - サブテキストにピンク {{PINK_LOGO_COLOR}}

4. C. パウンドケーキ販売箇所:
   - pound_cake_showcase_temp.png に差し替え
   - 「暫定画像、本物画像と後日差し替え予定」コメント明記

5. D. 占い機能:
   - 既存バブル弾き挙動を「手紙ポップアップ表示」に変更
   - data/marjorinMessages.ts に50件のメッセージ配列を実装
   - バブル弾き → ランダム1件抽選 → 手紙風ポップアップ(フェードイン+スケール+浮上)
   - 3枚カードのキャプション差し替え(1枚目・2枚目のみ、3枚目は現状維持)
   - 画像下に新リード文「心に響くスイーツ。あなたが輝くパッションを届けたい。」を追加

6. E. STORYセクション:
   - 見出しを「なぜマジョリンはお菓子を作ったのか」に変更
   - 3章構成に再編、第2章本文はプレースホルダー(TODO T4)

7. F. 「魔法で作ったもの〜」セクション:
   - 画像を hero_spring_02.png に差し替え
   - キャプションは現状維持

8. G. サイト全体:
   - 黄色アクセントを排除、ピンク+モノトーンで統一

完了後、各タスクの実装状況サマリーと、未解決のTODO一覧を返してください。
```

---

**※ 本書は v4.1(動画ファイル確定版)です。**
**※ 動画ファイル `opening.mp4` は Web 最適化済み(MP4/H.264/faststart/無音/1.63MB)で別途提供済み。**
**※ T1(ピンク確定値)と T4(STORY第2章本文)が来たら v4.2 で反映します。**
